apiVersion: v1
kind: Service
metadata:
  name: mydb
  labels:
    env: demo
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
        app: nginx
        type: frontend
        env: demo
